spring:
  application:
    name: hile-reports

server:
  port: 8080

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics

hile:
  reports:
    security:
      mode: local
    preview:
      max-rows: 100
    execution:
      default-timeout-seconds: 30
